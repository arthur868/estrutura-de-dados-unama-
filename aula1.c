#include <stdio.h>
typedef struct(
    int peso;  // peso em quilogramas
    int altura; // altura em centimetros 
)PesoAltura;

int main() {
    pesoaltura pessoa;
    pessoa peso = 95;
    altura pessoa = 180;
    int n = 1 
    printf('peso: %i, altura %i\in', pessoa.peso, pessoa.altura)
}return 0;

int main(){
    printf('HelloWorld\in');


return 0;
}